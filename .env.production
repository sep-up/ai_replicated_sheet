# 生产环境的环境变量（命名必须以 VITE_ 开头）

# 后端接口地址（如果解决跨域问题采用 CORS 就需要写绝对路径）
VITE_BASE_URL = /factory/v1

# 打包构建静态资源公共路径
VITE_PUBLIC_PATH = ./

# 网络请求地址代理路径
VITE_NGINX_PATH = /factory/v1

# base api（生产环境由 Nginx 代理，使用相对路径）
VITE_BASE_API = ''
